# Agent Catalog

This directory contains the repository-scoped agent contracts and skills for `template_madlib`.

- `AGENTS.md` defines the catalog contract.
- `skills/` contains the exemplar skill catalog.
- `skills/template-madlib/SKILL.md` is the operative workflow for config-owned token injection, conditional manuscript composition, artifact generation, and validation.

Keep skill metadata and instructions aligned with the live project version and source layout.
