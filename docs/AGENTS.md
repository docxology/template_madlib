# docs

This directory is for durable project-local documentation, not generated evidence.

Rules:

- Keep docs aligned with `manuscript/config.yaml`, `src/`, `data/claim_ledger.yaml`, and `domain_profile.yaml`.
- Describe generated outputs as regeneratable Stage 02-05 evidence, never as source.
- When documenting fork migration, name the required config, source, validator, test, pipeline, metadata, and claim-ledger changes.
- When documenting review packets, include hydrated Markdown, PDF, web output, slides, figures, data, reports, validation results, and copy statistics.
